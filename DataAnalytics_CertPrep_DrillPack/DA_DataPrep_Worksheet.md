# Data Analytics Drill: Prepare & Clean the Data

**Course**: Database Applications / Data Analytics (CTE) | **Instructor**: Ryan McMaster — Medina County Career Center  
**Rehearses the exam's data-preparation items — the ETL pipeline, joins, cleaning, outliers, and organizing data.**

---

## Part A — The Pipeline (ETL)

Name the phase — **Extract**, **Transform**, or **Load**:

1. Collecting the data from its source database → `____`
2. Validating, cleaning, and reshaping the retrieved data → `____`
3. Writing the finished data into the target warehouse → `____`

## Part B — Combine & Organize

4. You need to combine two datasets on a variable they share, keeping only matching rows. Which join? → `____`
5. You want to see only the rows where country = 'US'. This operation is called → `____`
6. Reordering rows from highest to lowest sales is called → `____`
7. Collapsing daily rows into monthly totals (sum per month) is called → `____`

## Part C — Clean It

8. A statistically valid rule to flag outliers: remove values more than ____ standard deviations from the mean. → `____`
9. A column stores dates as `2026-13-02`. What's wrong, and is this a cleaning issue? → `____________`
10. Three columns contain `NaN`, `None`, and `Unknown`. What single data-quality problem do all three represent? → `____________`

## Part D — Data Formats (XML)

Mark True/False:

11. XML tags are case-sensitive. → `T / F`
12. Every XML element needs an opening and a closing tag. → `T / F`
13. An XML document may have several root elements. → `T / F`
