# analytics03a Task: Build a Pizza Sales Dashboard

**Name: ______________________  Course**: Database Applications — Medina County Career Center

Using Tableau and `pizza_orders.csv`, build and submit a workbook (`.twbx`) or Tableau Public link.

## Build these views (separate sheets)
1. **Revenue by Region** — Region on Columns, SUM(Line Total) on Rows. Which region is highest? `________`
2. **Revenue by Size** — Size on Columns, SUM(Line Total) on Rows.
3. **Revenue Over Time** — Order Date (by Week) on Columns, SUM(Line Total) on Rows (a line chart).

## Assemble a dashboard
4. Combine all three views on one dashboard with a **title**.
5. Add a **Region filter** so the dashboard is interactive.

## Short answer
6. In Tableau, what is the difference between a **Dimension** and a **Measure**? `________________________`
7. What does adding a **filter** do for a dashboard viewer? `________________________`
8. Name one reason you'd use Tableau instead of Excel for this. `________________________`
